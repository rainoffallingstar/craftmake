schema_version: craftmake.standalone/v1
workflow:
  name: Smoke
  phase: step1
  backend: local
run:
  id: standalone-smoke
  project_dir: /tmp/craftmake-standalone-smoke
  state_dir: /tmp/craftmake-standalone-smoke/.craftmake
